"""
Test suite for ADAS dataset parsers and utilities.

- Contains tests for modules from src/adas/dataset
- Provides common helper functions for testing (e.g. assert_colored)
- Each subdirectory (e.g. dataset/) can have its own tests
- Launch: pytest tests/
- Dependencies: pytest, (optional) coverage, numpy, pandas, opencv-python
- Helper import: from tests.assert_colored import assert_colored
"""
