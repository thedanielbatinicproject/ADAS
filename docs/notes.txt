A. ROUTING SCENARIJA STREAMA:
CILJ
----
Napraviti “router” koji automatski bira režim rada (parametre i logiku) ovisno o:
- vidljivosti / vremenskim uvjetima (kiša, magla, snijeg, noć, odsjaj)
- dostupnosti i pouzdanosti prometnih traka (ima / nema / slabo vidljive)
- tipu podloge/ceste (asfalt, mokro, makadam) radi korekcije kočenja (μ-koef.)

Napomena: radimo heurističku procjenu kvalitete scene.
Router prvenstveno služi da: 
(1) stabilizira lane/obstacle detekciju, 
(2) ispravno “degrada” TTC/reakciju kad nema metrike.


1) VARIJABLE (FEATURES) KOJE RAČUNAMO IZ SLIKE
---------------------------------------------

1.1. Kvaliteta slike / vidljivost (globalne metrike)
- brightness_mean (prosjek luminancije) → noć / pothranjena ekspozicija
- brightness_p05_p95 (dinamički raspon) ili contrast_std → nizak kontrast = magla / odsjaj
- blur_laplacian_var (varijanca Laplaciana) → zamućenje / defokus / magla
- edge_density (udio Canny rubova) → nizak = magla/snijeg; previsok = šum/kiša
- saturation_mean (HSV S prosjek) → magla često snižava saturaciju
- glare_score (opcionalno): saturacija + “clipped highlights” (puno pikselā blizu 255)

Izvedene varijable:
- visibility_confidence (0..1): kombinacija kontrasta, rubova i blura
- is_night (bool): brightness_mean niska + malo rubova
- is_degraded (bool): visibility_confidence ispod praga

1.2. Pouzdanost prometnih traka (lane health)
- lane_found_left, lane_found_right (bool)
- lane_confidence (0..1): stabilnost kroz N frameova + konzistentna širina trake + paralelnost
- lane_width_px (u bird’s-eye) i njezina varijanca
- lane_tracking_stability: koliko se polinomi/linije “tresu” frame-to-frame

Izvedeno:
- has_lanes (bool): lane_confidence > threshold
- lanes_degraded (bool): lane_confidence nisko ali rubovi postoje (npr. kiša)

1.3. Tip ceste/podloge (heuristički) — opcionalno / “nice-to-have”
Cilj: procijeniti je li “loša” podloga (makadam/blato) ili mokro, radi povećanja zaustavne distance.

Heuristike (nesigurne; koristiti kao “probabilistic hint”):
- texture_roughness: visokofrekventna tekstura u ROI ispred vozila (makadam često “šumi”)
- color_uniformity: asfalt je često ujednačeniji od makadama
- specular_highlights: mokro → više odsjaja (više “clipped” ili sjajnih fleka)
- wheel-spray/rain streaks: teško pouzdano bez AI, ali može biti indikator degraded visibility

Izvedeno:
- road_surface_hint ∈ {ASPHALT_DRY, ASPHALT_WET, GRAVEL/UNPAVED, UNKNOWN}
- surface_confidence (0..1)

VAŽNO: ako surface_confidence nisko → NE donositi agresivne odluke, samo fallback.


2) REŽIMI RADA (MODES) I ŠTO SE MIJENJA
---------------------------------------

MODE 1: NORMAL_MARKED (trake OK, vidljivost OK)
- lane-based corridor + bird’s-eye homography
- distance u metrima (m/px kalibracija preko lane width)
- TTC = d / v_rel (tracking objekata u BEV)
- standardni pragovi za warning/brake

MODE 2: DEGRADED_MARKED (trake postoje ali vidljivost loša: kiša/magla/noć)
- jači preprocessing (npr. CLAHE / jači blur / prilagođeni Canny pragovi)
- lane_confidence smanjuje težinu (konzervativnija procjena)
- TTC i brzine: koristiti smoothing (EMA/Kalman) i veće margine
- pragovi reakcije konzervativniji (ranije upozorenje)

MODE 3: UNMARKED_GOOD_VIS (nema traka, ali slika OK)
- nema corridor-a → koristi se fiksni ROI/trapez ispred vozila
- udaljenosti su heurističke (pixel-space), ili “relative risk” umjesto metara
- koristi time-to-intersection u pixel-space (objekt raste / ulazi u ROI)

MODE 4: UNMARKED_DEGRADED (nema traka + loša vidljivost)
- minimalan set: motion-based obstacle warning u ROI
- izbjegavati tvrde TTC brojke; izdati upozorenje ako objekt brzo “prilazi” (scale change)
- fallback: “unknown risk” ako je slika preslaba

MODE 5: EMERGENCY_OVERRIDE
- neovisno o gore navedenom: ako objekt uđe u kritičnu zonu i closing rate visok
- odmah warning + kočenje prema pravilima


3) ROUTER LOGIKA (STATE MACHINE / PRAVILA)
------------------------------------------

Ulazi:
- visibility_confidence
- lane_confidence
- surface_hint + surface_confidence
- fps (da stabiliziramo brzine)

Pravila (pseudokod):
- if lane_confidence > T_lane and visibility_confidence > T_vis:
    mode = NORMAL_MARKED
- elif lane_confidence > T_lane_low and visibility_confidence <= T_vis:
    mode = DEGRADED_MARKED
- elif lane_confidence <= T_lane_low and visibility_confidence > T_vis:
    mode = UNMARKED_GOOD_VIS
- else:
    mode = UNMARKED_DEGRADED

Emergency override:
- if critical_object_in_roi and estimated_time_to_impact < T_emergency:
    mode = EMERGENCY_OVERRIDE

Histereza:
- promjena moda tek nakon K uzastopnih frameova (da ne “skače”)


4) UTJECAJ TIPA CESTE NA KOČENJE (μ i korekcija)
------------------------------------------------

Osnovno:
- zaustavni put ~ v^2 / (2 * μ * g)  (plus reaction distance)
- ako je podloga loša (manji μ) → povećati kočenje ranije ili smanjiti prag TTC

Praktično u sustavu:
- definirati koeficijent “braking_multiplier”:
    braking_multiplier = 1.0 (dry asphalt)
    braking_multiplier = 1.3..1.6 (wet asphalt)  [aproksimacija]
    braking_multiplier = 1.6..2.2 (gravel/unpaved) [aproksimacija]
- efekt u logici:
    effective_ttc_threshold = base_ttc_threshold * braking_multiplier
  ili
    required_braking_distance = nominal_distance * braking_multiplier

Napomena za pisanje rada:
- ovo je model aproksimacije; bez senzora ne možemo točno znati μ
- evaluirati osjetljivost: kako promjena μ utječe na stopu warning/brake


5) PREPROCESSING STRATEGIJE PO UVJETIMA (PARAMETRI)
----------------------------------------------------

NORMAL:
- Gaussian blur (mali)
- Canny: standardni pragovi

DEGRADED:
- CLAHE na luminanciji (povećanje kontrasta)
- jači blur za šum (kiša)
- adaptivni Canny pragovi (ovisno o kontrastu/edge_density)

NIGHT:
- pojačati brightness (gamma correction)
- agresivnije denoise
- oprez s false edges (farovi / refleksije)

6) TODO u kodu:

src/adas/context/
  __init__.py
  types.py
  scene_metrics.py
  lane_state.py
  road_surface.py
  router.py
  defaults.py

7) KAKO VALIDIRATI (EVALUACIJA)
-------------------------------

- Test scenariji:
  - jasno označene trake (dan)
  - kiša/magla/noć (degraded)
  - cesta bez traka (unmarked)
  - objekt ulazi sa strane (cut-in / pedestrian-like)

- Metrike:
  - precision/recall/F1 za detekciju prepreka u ROI/corridor
  - lane detection success rate + lane_confidence statistike
  - FPS
  - stabilnost moda (koliko često prebacuje; cilj: rijetko, uz histerezu)

- Dokumentirati ograničenja:
  - heuristike vremena/podloge nisu 100% pouzdane
  - bez senzora brzine i dubine: TTC je aproksimacija (osobito u UNMARKED modovima)


8) TERMINOLOGIJA ZA DOKUMENTACIJU
---------------------------------
- “Scenario routing” / “mode switching” / “context-aware pipeline”
- “visibility estimation” (heuristička) umjesto “weather classification”
- “lane availability” + “confidence”
- “unmarked road fallback”
- “friction coefficient μ” i utjecaj na kočenje


9) IMPLEMENTATION SNAPSHOT (usklađeno sa source kodom, 2026-06-01)
-------------------------------------------------------------------

Sažetak što je stvarno implementirano (za tekst rada):
- End-to-end orchestracija: src/adas/scenario/runner.py:26 (run_scenario).
- Asinkrona kontekstna analiza: src/adas/context/service.py:32 (ContextService).
- Router i histereza moda: src/adas/context/router.py:73 i src/adas/context/router.py:102.
- Lane detekcija (stateful processor): src/adas/lane_detection/processing.py:172 i :242.
- Degraded preprocessing (CLAHE/bilateral + prilagodbe):
  src/adas/lane_detection/processing.py:457.
- Obstacle detekcija + context-adaptive parametri:
  src/adas/obstacle_detection/detector.py:101 i :175.
- Procjena udaljenosti prepreke (perspektivna aproksimacija preko visine bbox-a):
  src/adas/obstacle_detection/detector.py:354.
- Risk estimator (TTC + score): src/adas/collision_risk/estimator.py:87.
- Decision sloj (WARN/BRAKE + road penalty): src/adas/collision_risk/decision.py:62.
- Vizualni overlayi: src/adas/ui/overlays.py:13, :52, :106.
- Stats panel: src/adas/ui/dashboard.py:32.
- Audio upozorenja: src/adas/ui/audio.py:27 i :47.
- Master dashboard ulaz: main.py:12, implementacija panela:
  src/adas/ui/master_panel.py:2649.

Važna napomena za tekst rada:
- Modul src/adas/lane_detection/hough.py je placeholder (nije aktivni put detekcije).
  Lane-availability heuristika je u src/adas/context/lane_heuristic.py:27.


10) TOČNE DEFAULT POSTAVKE KOJE TREBA NAVESTI U DOKUMENTACIJI
--------------------------------------------------------------

Kontekst (context/defaults):
- t_vis = 0.5 -> src/adas/context/defaults.py:18
- t_lane_low = 0.10 -> src/adas/context/defaults.py:122
- hysteresis_k = 5 -> src/adas/context/defaults.py:126
- braking multipliers (dry/wet/gravel/unknown) ->
  src/adas/context/defaults.py:135-138

Decision pragovi:
- warn_score_threshold = 0.35 -> src/adas/collision_risk/decision.py:47
- brake_score_threshold = 0.65 -> src/adas/collision_risk/decision.py:48
- ttc_warn_s = 3.0 -> src/adas/collision_risk/decision.py:49
- ttc_brake_s = 1.5 -> src/adas/collision_risk/decision.py:50
- road_penalty_warn = 0.08 -> src/adas/collision_risk/decision.py:51

Risk estimator:
- ttc_warning_s = 3.0, ttc_brake_s = 1.5 ->
  src/adas/collision_risk/estimator.py:68-69
- max_distance_m = 40.0 -> src/adas/collision_risk/estimator.py:71
- težine skora (proximity/ttc/lateral) ->
  src/adas/collision_risk/estimator.py:72-74

Obstacle detector:
- roi_top = 0.30, roi_bottom = 0.90 -> src/adas/obstacle_detection/detector.py:78-79
- min_area = 400 -> src/adas/obstacle_detection/detector.py:80
- mog2_var_threshold = 50.0 -> src/adas/obstacle_detection/detector.py:85
- focal_length_px = 700.0, assumed_object_height_m = 1.5 ->
  src/adas/obstacle_detection/detector.py:90-91


11) RAZLIKE U ODNOSU NA POČETNI DIZAJN (objasniti u radu kao odluke)
--------------------------------------------------------------------

- Udaljenost prepreke nije primarno mjerena preko lane-width kalibracije,
  nego preko heuristike visine objekta i žarišne duljine:
  src/adas/obstacle_detection/detector.py:354.

- Emergency mode je podržan kroz context router override (EmergencySignal),
  ali u glavnom flowu često je važnija kombinacija risk + decision sloja.
  Referenca: src/adas/context/router.py:244 i src/adas/collision_risk/decision.py:62.

- Dio lane-aware logike je namjerno podijeljen:
  lane existence heuristika u context paketu, geometrija trake u lane_detection paketu.
  Reference: src/adas/context/lane_heuristic.py:27, src/adas/lane_detection/processing.py:242.


12) SKRIPTE KOJE TREBA NAVESTI U POGLAVLJU "IMPLEMENTACIJA"
------------------------------------------------------------

- Glavni scenario CLI: scripts/run_scenario.py
  - parser argumenata: scripts/run_scenario.py:31
  - normal mode: scripts/run_scenario.py:421
  - streaming mode za konfigurator (latest.jpg + status.json):
    scripts/run_scenario.py:57

- Debug lane pipeline: scripts/debug_lanes.py:99 (main)
- Debug obstacle pipeline: scripts/debug_obstacles.py:98 (main)
- Kontekstna analiza videa (tablica/GUI): scripts/context/analyze_video.py:960 (main)


13) TEST REFERENCE (za poglavlje "Testiranje")
-----------------------------------------------

- Router decision + histereza:
  tests/context/test_router.py:24, :106, :123
- Lane processing:
  tests/lane_detection/test_processing.py:55
- Obstacle detector + tracker:
  tests/obstacle_detection/test_detector.py:57, :132
- Risk estimator:
  tests/collision_risk/test_estimator.py:104
- Decision logic:
  tests/collision_risk/test_decision.py:33
- Scenario run (dry run):
  tests/scenario/test_runner.py:115
- UI overlayi:
  tests/ui/test_overlays.py:21, :52


14) PREPORUKA ZA PISANJE POGLAVLJA (main.tex / chapters)
--------------------------------------------------------

Preporučena struktura narativa:
- "Context-aware routing": prvo objasniti scene metrics i visibility,
  zatim lane_state, pa mode switching + histerezu.
- "Perception": lane detection i obstacle detection kao odvojene faze,
  s naglaskom na context-adaptive parametre.
- "Risk and action": TTC/risk_score pa decision thresholds i road penalty.
- "UI and tooling": dashboard, player backends, debug skripte, streaming mode.
- "Validation": mapirati svaku fazu na postojeće test fajlove i metrike.

Time se direktno povezuju implementacija i teorijski dio rada, bez rupe između
planiranog dizajna i stvarnog ponašanja koda.