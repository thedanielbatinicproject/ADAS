services:
  adas:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    environment:
      - PYTHONPATH=/app/src
    ports:
      - "8888:8888"
    command: sleep infinity
    cpus: "4"
    mem_limit: "8g"