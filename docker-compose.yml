services:
  adas:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    environment:
      - PYTHONPATH=/app/src
    ports:
      - "8888:8888"
    command: sleep infinity
    deploy:
      resources:
        limits:
          cpus: "4"
          memory: "8g"